[project]
name = "command-schema-discovery"
version = "0.1.0"
description = "Python wrapper for the command-schema-discovery CLI"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
